
const INITIAL_STATE = {
  currentProfile: null,
  loading: true,
  error: {},
};

export const profileReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default :
      return state;
  };
};